const Navbar = () => {
    const nav = document.createElement("nav"); //Creamos un elemento.
    nav.classList.add("navbar"); //Creamos una clase.
    const divNav = document.createElement("div");
    divNav.classList.add("menu");
 
 
}

export default Navbar